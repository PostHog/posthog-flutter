---
"posthog_flutter": minor
---

Add `$recording_status` and `$sdk_debug_*` session replay debug properties to captured events via the native SDKs, forward `throttleDelay` to the native replay config, and raise the native floors to posthog-ios 3.80.0 and posthog-android 3.69.0
