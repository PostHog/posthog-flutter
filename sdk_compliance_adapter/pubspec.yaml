name: posthog_flutter_sdk_compliance_adapter
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  posthog_flutter:
    path: ../posthog_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
